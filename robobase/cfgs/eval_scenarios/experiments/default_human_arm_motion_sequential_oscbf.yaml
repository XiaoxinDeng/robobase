# @package _global_

extends:
  - ../default_human_arm_motion.yaml
  - ../methods/sequential_oscbf.yaml

scenario:
  name: default_human_arm_motion_sequential_oscbf
  description: >-
    ACT plus sequential OSCBF chunk filtering in the default human-arm motion environment.

hydra:
  run:
    dir: eval_safety/${scenario.name}_${now:%Y%m%d_%H%M%S}

environment: {}

safety_filter: {}

eval_args: {}
