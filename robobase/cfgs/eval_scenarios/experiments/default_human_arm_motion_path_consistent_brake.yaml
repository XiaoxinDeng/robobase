# @package _global_

extends:
  - ../default_human_arm_motion.yaml
  - ../methods/path_consistent_brake.yaml

scenario:
  name: default_human_arm_motion_path_consistent_brake
  description: >-
    ACT plus path-consistent braking in the default human-arm motion environment.

hydra:
  run:
    dir: eval_safety/${scenario.name}_${now:%Y%m%d_%H%M%S}

environment: {}

safety_filter: {}

eval_args: {}
